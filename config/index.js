export const express = {
  // Set the server port number to 3000
  port: 3000,
}

export const db = {
  // Set the mongodb uri to local mongodb uri
  mongoUri: "mongodb://localhost:27017",
  // Set the database name as "Marketplace"
  dbName: "Marketplace"
}
